doctype html
html(lang="en")
    head
        include partials/header
    body.login
        .contenedor-formulario
            h1 Crea tu cuenta de Usuario
            //- paso 5 (el paso 4 esta en usuariosController y el paso 1, 2 y 3 están en el index)
            if locals.mensajes
                .alertas 
                    each categoria in Object.keys(locals.mensajes)
                        each error in mensajes[categoria]
                            .alerta(class=`${categoria}`)
                                p= error                                                                                                    

            form#formulario.caja.caja-login(method="POST" action="/crear-cuenta")
                .campo
                    label(for="email") E-mail:
                    input(type="email", name="email" placeholder="E-mail" value= email)
                .campo
                    label(for="password") Password:
                    input(type="password", name="password" placeholder="Password" value= password)
                .campo.enviar
                    input.boton(type="submit", value="Crear Cuenta")
                .campo
                    a(href="/iniciar-sesion") Volver a Iniciar Sesión 
